选择 **Mamba/SSM 状态空间模型** 和 **线性注意力** 两种方案作答，这两者都是当前长上下文建模的主流方向，且技术原理对比鲜明 👇

---

## 方案一：Mamba / SSM 状态空间模型

### 核心技术原理

SSM 的本质是用一个**压缩的隐状态（Hidden State）**来"记忆"历史信息，而非存储所有历史 token。

其核心离散化递推公式为：

$$h_t = \bar{A} h_{t-1} + \bar{B} x_t$$

$$y_t = C h_t$$

其中：
- $$h_t$$ 是固定维度的**隐状态向量**，压缩了所有历史信息
- $$\bar{A}, \bar{B}, C$$ 是可学习的状态转移矩阵
- $$x_t$$ 是当前输入，$$y_t$$ 是当前输出

**Mamba 的关键创新**在于引入了**选择性机制（Selective State Space）**：让 $$\bar{B}, C$$ 和步长 $$\Delta$$ 都依赖于输入 $$x_t$$ 动态生成，使模型能够**选择性地遗忘或记忆**信息，克服了传统 SSM 表达能力不足的问题。

### 相较于传统全量注意力的优势

| **维度** | **全量注意力（Full Attention）** | **Mamba/SSM** |
|----------|-------------------------------|---------------|
| 时间复杂度 | $$O(n^2)$$ | $$O(n)$$ |
| 空间复杂度 | $$O(n^2)$$（KV Cache） | $$O(d)$$（固定隐状态） |
| 推理内存 | 随序列长度线性增长 | **常数级**，不随长度增长 |
| 长序列处理 | 极长序列下显存爆炸 | 天然支持**无限长序列**流式推理 |

---

## 方案二：线性注意力（Linear Attention）

### 核心技术原理

传统 Attention 的计算瓶颈在于 Softmax：

$$\text{Attention}(Q,K,V) = \text{softmax}\left(\frac{QK^T}{\sqrt{d}}\right)V$$

其中 $$QK^T$$ 的计算是 $$O(n^2)$$ 的。

线性注意力的核心思路是：**用核函数 $$\phi(\cdot)$$ 替换 Softmax**，将相似度分解为两个向量的内积：

$$\text{sim}(Q_i, K_j) \approx \phi(Q_i)^T \phi(K_j)$$

利用矩阵乘法结合律，将计算顺序从 $$(QK^T)V$$ 改为 $$Q(K^TV)$$：

$$\text{Output}_i = \frac{\phi(Q_i)^T \sum_{j} \phi(K_j) V_j^T}{\phi(Q_i)^T \sum_{j} \phi(K_j)}$$

其中 $$S = \sum_j \phi(K_j) V_j^T$$ 可以**递推累积**，无需存储完整的 $$n \times n$$ 注意力矩阵。

常见核函数选择：$$\phi(x) = \text{elu}(x) + 1$$（RetNet、GLA 等均基于此思路）。

### 相较于传统全量注意力的优势

| **维度** | **全量注意力** | **线性注意力** |
|----------|--------------|--------------|
| 训练复杂度 | $$O(n^2 d)$$ | $$O(n d^2)$$，当 $$d \ll n$$ 时大幅降低 |
| 推理模式 | 需维护完整 KV Cache | 可转化为 **RNN 递推形式**，推理为 $$O(1)$$ |
| 并行训练 | ✅ 支持 | ✅ 支持（矩阵形式并行） |
| 长文本扩展 | 受限于显存 | **线性扩展**，支持超长上下文 |

---

## 💡 综合对比总结

两种方案都从根本上打破了全量注意力 $$O(n^2)$$ 的复杂度瓶颈：

- **Mamba/SSM**：完全抛弃注意力机制，用状态递推替代，推理极致高效，但对**局部精确匹配**能力略弱于注意力。
- **线性注意力**：保留注意力的形式框架，通过核函数近似降低复杂度，**与 Transformer 生态兼容性更好**，但核函数近似会损失部分表达能力。

两者的共同优势是：在超长上下文场景（如 128K+ tokens）下，**计算和内存开销远低于传统全量注意力**，是当前长文本大模型的核心技术路线。
